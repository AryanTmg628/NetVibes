import { Stack } from "@mui/material"
import { SearchDomain } from "../../../components/search-domain/search-domain"
import { DomainSearch } from "../../domain/components/domain-search/domain-search"

export const RegisterDomain = () => {
  return (
  <Stack>
      <DomainSearch />
  </Stack>
  )
}

