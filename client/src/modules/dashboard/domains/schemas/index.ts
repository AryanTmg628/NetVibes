import { credentialFormSchema } from "./credential-form-schema";
import { dnsConfigurationFormSchema } from "./dns-configuration-form";

const domainSchemas = {
  credentialFormSchema,
  dnsConfigurationFormSchema,
};
export default domainSchemas;
